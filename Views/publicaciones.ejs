<!DOCTYPE html>
<html>
  <head>
    <title>Red Social</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Red Social</h1>
    <% if (mensaje) {%><h3><%= mensaje.message %></h3>
    <%} else {%>
        <ul>
        <% for(let i = 0; i < publicaciones.length; i++) {%>
            <li>
                <a href="/publicacion/<%= publicaciones[i].id %>"><%= publicaciones[i].titulo %></a>
                <p><%= publicaciones[i].descripcion %></p>
            </li>
        <%} %>
        </ul>
    <%} %>
  </body>
</html>
